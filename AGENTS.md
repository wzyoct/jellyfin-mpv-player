# 项目 Agent 规则

## 沟通

- 默认使用简洁、清晰的中文回复；用户指定其他语言时遵从用户。
- 先给结论，再说明必要依据。多步骤任务开始工具操作前，用一两句话说明当前动作。
- 信息缺失但存在低风险合理假设时，简短说明并继续；只有在会实质改变结果或带来明显风险时才提问。
- 只处理用户要求及直接必要的事项，不添加无关功能、臆测性重构或泛泛的后续建议。

## 执行

- 先读取相关文件和上下文。简单修改直接执行；非简单任务先给出简短计划，包含原因、影响范围、方案和验证方式。
- 优先定位根因并做最小、聚焦的改动；不要用重复逻辑、第二数据源、静默回退或吞错来掩盖问题。
- 涉及重复业务逻辑、共享校验/权限/路由、接口契约、跨模块状态、安全或数据完整性时，按结构性问题处理，统一不变量并清理过时逻辑。
- 保持函数短小、嵌套扁平、职责清晰；注释只说明意图或取舍，不重复代码。
- 保留用户已有改动；未经明确要求不得使用破坏性命令。
- 独立子任务可并行；涉及相同文件或有依赖时按顺序执行。

## 安全与环境

- 不硬编码密钥、凭据或令牌；数据库使用参数化查询；在系统边界校验和清理外部输入。
- Windows 访问 Linux 主机 `[redacted-local-address]` 时优先使用 `http://[redacted-local-address]:<port>`。
- 仅在任务确实需要且普通权限不足时使用管理员权限；确需且本机缺失的通用工具可直接安装。

## 验证与收尾

- 修改后按适用情况依次运行：针对性测试、类型检查或 lint、构建、最小冒烟验证；后端单元测试硬超时 60 秒。
- 验证失败时保留清晰错误，不伪造成功；无法验证时说明原因和下一步最佳检查。
- 完成前检查差异，关注重复逻辑、隐藏回退、吞错、死代码、未说明的行为变化、测试不足和安全回归。
- 技能目录存在匹配技能时，先读取其 `SKILL.md` 并遵循；使用该技能时告知用户。

## Git 存档

- 任何产生文件改动的任务，都必须在验证和差异检查完成后创建 Git 提交，不允许只修改工作区而不留存档。
- 只暂存本次任务相关文件，不混入用户已有或无关改动；提交前检查 `git diff` 和 `git diff --cached`。
- 提交信息使用清晰的 Conventional Commits 格式，例如 `fix: 修复媒体库加载`、`feat: 重做首页内容区`。
- 较大任务按可独立验证、可独立回滚的阶段分成多个提交，不把功能修复、UI 重做和打包产物混在一个提交中。
- 提交后推送当前分支到 `origin`，确保本地存档同时具备远程备份；不得提交令牌、密码、本机配置、日志、依赖和构建产物。
- 未经用户明确要求，不改写已推送历史，不强制推送，不删除分支或标签。

## 版本与更新日志

- 任何面向用户的更新都必须同步递增 `package.json` 和 `package-lock.json` 的版本号，并补充结构化更新记录。
- `src/data/release-notes.json` 是版本记录的单一来源；`CHANGELOG.md` 必须通过发布脚本从该文件生成，禁止只保留 `Unreleased` 而不结算正式版本。
- 发布前必须运行 `npm run release:check`，确保包版本、锁文件、更新记录和客户端运行时版本一致。
- 桌面端用户可见改动在源码验证和构建后，必须继续运行 `npm run release:windows` 重新生成 `release/Ember Player.exe`；交付前核对便携版启动说明中的版本号和 exe 的更新时间，不能只交付 `dist` 或 `dist-electron`。
