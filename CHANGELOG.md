# Changelog

## 0.9.0 - 2026-09-01

以 Jellyfin 为主完成媒体服务器兼容，同时保留现有 Emby 连接与播放能力。

### 新增

- 支持 Jellyfin 10.11.x 的账号密码登录、媒体库浏览、图片、详情、播放信息和进度同步。
- 连接时自动识别 Jellyfin 或 Emby，并展示服务器名称和版本；Jellyfin 使用标准 Authorization 鉴权。
- 保留旧版 Emby 设置和加密令牌，升级后无需重新登录。

### 优化

- 连接页和默认提示以 Jellyfin 为主，同时明确兼容 Emby。
- 保留 Jellyfin 自定义 Base URL，不再把服务器根地址强制改写为 /emby。
- 允许无密码媒体服务器账号登录，并在切换服务器时清理旧凭据和图片缓存。

## 0.8.7 - 2026-09-01

融合首页沉浸式浏览体验，并修复继续观看海报偶发停留在加载占位的问题。

### 优化

- 首页导航、推荐横幅和首个内容轨道融合为连续的沉浸式首屏，滚动后顶栏恢复清晰的深色背景。
- 继续观看中的剧集海报按剧集、季、单集和父级缩略图顺序回退，减少媒体元数据不完整时的空白卡片。

### 修复

- 修复媒体列表刷新或 eager/lazy 状态切换后海报组件永久显示加载占位的问题。
- 合并首页相同图片的并发请求，并在图片请求超时或解码失败时正确进入回退或缺图状态。

## 0.8.6 - 2026-09-01

优化首页剧情简介、横幅清晰度和全屏浏览体验。

### 新增

- 顶栏增加原生全屏切换按钮，支持 F11 进入或退出、Esc 退出全屏。

### 优化

- 首页推荐轮播直接展示 Emby 剧情简介，不再显示通用占位提示。
- 首页横幅按最高 3840px 宽度请求 Emby 背景图，提升高分辨率窗口的显示清晰度。

## 0.8.5 - 2026-09-01

改用 MPV 原生播放列表，稳定实现电视剧跨集连播和选集。

### 新增

- 整部剧集预检后一次性加载到 MPV 原生播放列表，支持跨季自动连播、上一集、下一集和 MPV 内选集。
- 播放列表使用内存 M3U 和 MPV 原生请求头，不在播放地址中暴露 Emby Token。
- MPV 右键和 Ctrl+E 打开内置选集列表，播放条同步完整队列序号和当前剧集。
- 预检失败或请求头不兼容的剧集显示持久化跳过警告。

### 修复

- 移除 EOF 后临时请求下一集的竞态链路，按 MPV playlist 条目 ID 串行处理播放生命周期和 Emby 上报。
- MPV 路径验证和启动播放要求 0.41 或更新版本，并在运行时加载失败时显示具体剧集和错误。

## 0.8.4 - 2026-09-01

修复 MPV 路径验证与桌面桥接兼容性。

### 修复

- 修复沙箱 preload 加载本地模块失败导致 window.emby 未暴露、MPV 验证按钮报错的问题。
- MPV 路径统一兼容首尾空白、单双引号、重复反斜杠、正斜杠和包含空格的 Windows 路径。
- 配置保存、路径验证、测试启动和真实播放统一使用规范化后的 MPV 路径。

## 0.8.3 - 2026-09-01

修复首页轮播操作区与 MPV 播放调用，并增加本地诊断日志。

### 修复

- 轮播播放和查看详情按钮改为统一操作区，修复基线错位、拥挤和窄窗口布局问题。
- MPV 布尔属性改用 set_property，修复播放初始化时的 invalid parameter 错误。
- MPV 请求头通过 IPC 原生数组设置，不再重复放入启动参数。
- 播放错误移除 Electron 远程调用包装，显示可读的中文上下文。

### 新增

- 增加本地脱敏 JSONL 滚动日志，记录 Emby、MPV、播放会话、窗口和异常诊断信息。
- 设置页增加打开日志目录入口，日志保存在便携版 data/logs 并自动限制占用空间。

## 0.8.2 - 2026-09-01

完善媒体展示过渡，并强化 MPV 续播与电视剧连播。

### 优化

- 统一剧集标题为“第几集 - 集名”，详情、卡片、搜索和播放状态保持一致。
- 背景图支持无图片标签时按单集、季度、剧集和主海报顺序尝试加载。
- Hero 轮播改为预加载后的双图层交叉淡化，并同步过渡文字内容。

### 修复

- MPV 续播跳转后读取实际 time-pos，确认恢复到目标位置后才开始播放。

## 0.8.1 - 2026-08-31

固定字幕与电视剧连播默认行为，修复续播进度和电视剧背景图。

### 修复

- 移除电视剧连播和中文字幕偏好勾选项，改为固定默认行为。
- 播放前使用 Emby 最新进度，并在 MPV 文件加载完成后精确恢复播放位置。
- 电视剧按正片顺序跨季自动连播，切集时为每集独立解析媒体源和字幕。
- 背景图按单集、季度、剧集顺序继承，最终使用剧集主海报填充。
- 修正 Emby 播放上报字段并让发布流程运行完整单元测试。

## 0.8.0 - 2026-08-31

统一播放会话、支持电视剧连续播放，并改为根目录免安装便携版。

### 新增

- 播放会话统一由快照和递增版本驱动，MPV 关闭后当前页面即时刷新。
- 电视剧按季集顺序连续播放，支持跨季、上一集、下一集和播完本集停止。
- 首页新增下一集轨道和底部播放控制坞，Hero 主按钮可直接播放或继续播放。
- Windows 发布改为 release 根目录解包便携版，配置和缓存统一存入 data 文件夹。

### 修复

- MPV 在 IPC 连接完成后再载入媒体，避免启动阶段丢失播放事件。
- 每集独立解析媒体源、音轨和字幕，并补充 Emby 播放列表上报字段。
- 增加快照恢复和有界重查，避免播放结束后必须重新进入应用才能看到最新状态。

## 0.7.0 - 2026-08-31

提升继续观看的进度可靠性，并优化剧集信息、媒体卡片和详情交互。

### 新增

- 通过 MPV JSON IPC 长连接读取播放位置、时长和暂停状态。
- 在播放中、暂停、跳转、退出和自然结束时同步 Emby 播放进度。
- 播放结束后自动刷新继续观看内容，并保留完整的单集上下文。

### 优化

- 单集卡片优先显示第几集，副标题显示所属剧集和季度。
- 重做媒体卡片进度条、海报悬停反馈和详情弹窗过渡。
- 增加 MPV IPC、Emby 请求和详情请求的错误提示与竞态保护。

## 0.6.2 - 2026-08-31

整理 Windows 发布目录，打开即可看到当前版本安装包。

### 优化

- 将当前版本的安装 EXE 和绿色版 ZIP 直接放在 release 根目录。
- 将历史版本和构建内部文件单独归档，减少普通用户的查找成本。

## 0.6.1 - 2026-08-31

修复媒体库网格留白和继续观看海报显示。

### 修复

- 让媒体库海报网格按可用宽度均分列，统一左右留白。
- 让继续观看中的单集使用所属剧集的主海报。

## 0.6.0 - 2026-08-31

整理 Windows 发布包和启动入口。

### 新增

- 增加带编号的 Windows 安装版和免安装绿色版发布入口。
- 自动生成启动说明、版本更新记录和 SHA256 文件校验值。

### 优化

- 将当前版本、历史版本和构建内部文件分开存放，让发布目录更容易识别。
- 统一发布前的版本检查、测试、构建和整理流程。

## 0.5.0 - 2026-08-31

增加版本更新记录子页。

### 新增

- 增加“更新”导航入口，集中展示每一次正式版本的详细更新内容和日期。
- 增加当前版本标识和按时间倒序排列的版本时间线。
- 增加版本号、锁文件和更新记录的一致性校验。

### 优化

- 将界面版本号、Emby 客户端请求标识和发布记录统一到同一套版本来源。

## 0.4.0 - 2026-08-31

重做首页媒体浏览体验。

### 新增

- 增加电影推荐轮播，支持自动切换、手动切换和悬停暂停。
- 增加继续观看、最近加入、Movie、Show、All 内容轨道。
- 增加服务端完整续播和媒体库数据加载。

### 优化

- 统一沉浸式窗口顶栏与原生窗口控制按钮的视觉表现。
- 隐藏内容轨道横向滚动条，改用左右箭头进行浏览。

### 修复

- 修复媒体数据来源分散导致的续播内容不完整问题。

## 0.3.0 - 2026-08-31

### 发布

- 将 Windows 发布主目标改为 NSIS 安装版和 ZIP 绿色版，避免单文件 portable 每次启动重复解压。
- 移除外部字体请求、裁剪 Electron 语言包和重复打包的前端依赖，加快首屏显示并缩小运行时。
- 集成隐藏式窗口标题栏、全宽 Hero 和内容轨道，改善超宽屏留白与滚动条观感。
- 增加按视口加载海报，详情页支持响应式宽度；字幕默认优先外挂字幕，其次中文字幕。

## 0.2.0 - 2026-08-31

### 发布

- 修复 Emby 媒体库视图响应解析，恢复登录后的首页、电影和剧集加载。
- 增加媒体库筛选、分页、请求竞态保护，以及加载中、空内容、错误和重试状态。
- 统一媒体卡片和搜索状态，优化桌面端深色视觉层级、焦点态和无图占位。
- 增加图片加载失败回退与有容量限制的图片缓存。
- 增加 Emby 接口契约冒烟测试和 Windows portable 打包验证。
